{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'review.new'|trans }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ 'review.submit'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
